import { LogoutBtn } from "./LogoutBtn";
import { render } from "@testing-library/react";

describe("LogoutBtn", () => {
  it("renders component", () => {
    const {} = render(<LogoutBtn />);
  });
});
